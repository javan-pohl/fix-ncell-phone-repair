import * as React from 'react'
import { Link } from 'gatsby'
import { styles } from './Header.module.css'

const Header = () => {
	return (
		<div className={styles.header}>this is my header</div>
	)
}